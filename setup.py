#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='GoCamp',
      version='0.1',
      description='A better way to find campsites',
      author='Jon Peck',
      author_email='peckjon@gmail.com',
      url='https://github.com/peckjon/',
      packages=find_packages(),
     )
